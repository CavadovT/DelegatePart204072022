using System;
using System.Collections.Generic;
using System.Text;

namespace UtulsLibrary.Enums
{
    public enum Role { admin = 1, member }
    public enum MenyuBar { Quit, AddBook, GetBookById, GetAllBooks, DeleteBookById, EditBookName, FilterByPageCount, }
}
